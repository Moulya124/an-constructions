/**
 * main.js
 * Site-wide behavior that isn't specific to loading partials.
 * Add page-specific scripts here as sections get built out
 * (project filtering, contact form handling, etc.).
 */
document.addEventListener('DOMContentLoaded', () => {
  // placeholder — extend in later steps
});
